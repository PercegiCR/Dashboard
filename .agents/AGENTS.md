# Auto-Commit and Push Changes
- Every time you finish making updates or code modifications, you MUST automatically stage, commit, and push the changes to the Git repository.
- Use the `run_command` tool to execute: `git add . && git commit -m "<brief description of updates>" && git push`
- After executing the git commands, you MUST explicitly inform the user in your message that the changes have been committed and pushed.
